export type FormData = {
	startDate: string;
	endDate: string;
	country: string;
	region: string;
	city: string;
};

export type Destination = {
	ip: string;
	country: string;
	region: string;
	city: string;
};

export type SelectItem = {
	value: string;
	label: string;
};
